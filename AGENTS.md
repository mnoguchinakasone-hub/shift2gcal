# Project Overview

**プロジェクト名**: 要記入
**目的**: 要記入
**ステータス**: ドラフト

## Tech Stack

- 言語: **今後調整**
- フレームワーク: **今後調整**
- パッケージマネージャ: **今後調整**
- テスト: **今後調整**

## Architecture

- 構成概要: **今後調整**
- 主要ディレクトリ: **今後調整**

## Commands

```bash
# 依存関係インストール
# 要記入

# 開発サーバ / 実行
# 要記入

# lint
# 要記入

# test
# 要記入
```

## Coding Rules

- 既存のコードスタイル・命名規則に従う
- 依頼範囲外のリファクタやファイル追加をしない
- 不明な仕様は勝手に仮定せず、確認する

## Testing Rules

- 変更に応じて関連テストを実行する
- テスト追加が必要な場合は理由を説明する

## Security Rules

- APIキー・トークン・認証情報を外部へ送信しない
- `.env` や秘密情報ファイルの内容を読み出さない
- 個人情報をプロンプトやログに含めない
- `git push` や破壊的な git 操作を勝手に実行しない
- 危険な削除（例: 広範囲の `rm`）を実行しない

## Git / PR Rules

- コミットは明示的に依頼されたときのみ行う
- コミットメッセージは「なぜ」を重視して簡潔に書く
- PR作成も明示依頼時のみ行う

## Important Constraints

- このファイルは Cursor / Claude Code 共通ルールの**唯一の正本**である
- `CLAUDE.md` に中身は持たない。Claude Code 用の入口として本ファイルへのシンボリックリンクのみ置く
- Claude Code 固有ルールは `.claude/` 配下（主に `.claude/rules/`）へ分離する
- このファイルには「常に守ってほしいこと」だけを書く
- 長い作業手順は Skill へ移す
- 特定パス・言語限定のルールは Rules へ移す
- 一時的な好みや一度きりの訂正は Auto Memory へ任せる

## ヒアリングが必要な項目（不明事項リスト）

### 1. プロジェクト固有

- [ ] デプロイ先
- [ ] 環境変数の管理方法
- [ ] レビュー必須条件

---

**作成日**: YYYY-MM-DD
**最終更新**: YYYY-MM-DD
**ステータス**: ドラフト
