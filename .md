開発要件、データ構造の解析結果、および処理設計の全体まとめです。

**システム概要とアーキテクチャ**

* **開発形式**: Chromeブラウザ拡張機能（Manifest V3）
* **データ取得元**: 業務管理アプリのバックエンド通信（GET APIのJSONレスポンス）


* **連携先**: GAS（Google Apps Script）または Google Calendar API 経由でGoogleカレンダーに反映

**データ取得仕様（デフォルト ＋ 差分オーバーライド方式）**
画面のDOM（HTML）解析ではなく、APIレスポンスに含まれる差分JSONデータを利用して処理を分岐します。

| シフト区分 | APIデータ仕様 (`type` / `work_start`) | カレンダー登録内容 |
| --- | --- | --- |
| **通常勤務** | **APIデータの配列に含まれない** | スクリプト側で設定した基本時間（例: 09:00〜18:00）で登録 |
| **時間変更** | `type: "work_input"` / 時間指定あり

 | `work_start` 〜 `work_end` の日時で登録

 |
| **半休** | `type: "public_holiday_half_work"` / 時間指定あり

 | `work_start` 〜 `work_end` の日時で登録（午前/午後自動対応）

 |
| **公休・定休** | `type: "public_holiday"` / `work_start: null`<br> | 登録スキップ、または終日イベント「公休」として登録

 |

**メイン処理フロー**

1. **対象月の全日付生成**: 拡張機能側で対象月（例: 10月1日〜31日）の日付配列を作成。
2. **APIデータのハッシュマップ化**: 取得した差分JSON配列を `target_day`（`YYYY-MM-DD`）をキーにした連想配列に変換。


3. **ループ処理と時刻補完**:
* 日付データがマップ内に存在する ➔ `work_start` / `work_end`（ISO 8601形式）からイベント日時を抽出。


* 日付データがマップ内に存在しない ➔ デフォルト勤務時刻を自動セット。


4. **カレンダー同期・重複登録防止**: 送信前に同月の既存同期イベントを上書き削除（または拡張プロパティでID管理）して登録。
